[pytest]
# 게이트 스코프 = 살아있는 테스트 전부. 좁히면 수집 밖에 숨는 테스트가 생기고,
# 그건 초록도 빨강도 아닌 채로 "통과"에 섞인다.
testpaths = tests evals
norecursedirs = .git .venv venv node_modules __pycache__ .claude worktrees _migration
python_files = test_*.py
